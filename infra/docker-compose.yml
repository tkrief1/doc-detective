services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: docd
      POSTGRES_PASSWORD: docd
      POSTGRES_DB: docd
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
